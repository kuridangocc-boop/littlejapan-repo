# === Server Settings ===
PORT=4000

# === Admin Credentials ===
ADMIN_USER=admin
ADMIN_PASS=yourpassword

# === JWT Settings ===
JWT_SECRET=changeme
JWT_EXPIRES_IN=1d

# === EFS or Local Storage Directories ===
UPLOAD_DIR=/mnt/efs/uploads
CONVERTED_DIR=/mnt/efs/converted

# === Database (Optional SQLite) ===
DB_FILE=/mnt/efs/db/littlejapan.db
